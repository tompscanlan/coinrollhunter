# Personal data — never commit. Users bring their own.
pm_holdings.json
crh_ledger.json
*.db
*.sqlite
*.sqlite3
data/

# Secrets
.env
.env.local

# Build output. web/dist IS committed — it's the embedded UI, so `go build`
# and `go install` work without Node. node_modules and release artifacts aren't.
node_modules/
/coinrollhunter
/coinrollhunter.exe
/dist/

# Python
__pycache__/
*.pyc
.venv/

# OS / editor
.DS_Store
Thumbs.db
*.xlsx
.idea/
.vscode/
